# ============================================================
# TUSA TRADE — НАСТРОЙКИ
# ============================================================

# Монеты для сканирования (данные с Binance, реальное время).
# Слева — символ Binance, справа — как пара называется в Pocket Option.
# ВАЖНО: ставить только обычные пары, НЕ OTC!
PAIRS = {
    "BTCUSDT": "BTC/USD",
    "ETHUSDT": "ETH/USD",
    "SOLUSDT": "SOL/USD",
    "XRPUSDT": "XRP/USD",
    "ADAUSDT": "ADA/USD",
    "DOGEUSDT": "DOGE/USD",
    "BNBUSDT": "BNB/USD",
    "LTCUSDT": "LTC/USD",
    "AVAXUSDT": "AVAX/USD",
    "LINKUSDT": "LINK/USD",
}

# Валютные пары (данные с Deriv, реальное время, без ключей).
# Слева — символ Deriv, справа — как пара называется в Pocket Option.
FOREX_PAIRS = {
    "frxEURUSD": "EUR/USD",
    "frxGBPUSD": "GBP/USD",
    "frxUSDJPY": "USD/JPY",
    "frxAUDUSD": "AUD/USD",
    "frxUSDCAD": "USD/CAD",
    "frxUSDCHF": "USD/CHF",
    "frxEURJPY": "EUR/JPY",
    "frxGBPJPY": "GBP/JPY",
}

# Экспирация: бот сам выбирает от MIN до MAX минут под характер сетапа.
# Откат (быстрый отскок) — короче; сильный тренд — длиннее.
EXPIRY_MIN = 1
EXPIRY_MAX = 3
# Старое имя оставлено для совместимости (запасной дефолт)
EXPIRY_MINUTES = 1

# ---------- Само-калибровка по живой статистике ----------
# Бот сам отключает тактику/пару, если её винрейт просел ниже безубытка.
CAL_WINDOW = 30        # сколько последних сделок учитывать (скользящее окно)
CAL_MIN_SAMPLE = 15    # минимум сделок, прежде чем судить
CAL_BREAKEVEN = 0.55   # ниже этого винрейта (доля) — отключаем (безубыток ~0.54)
CAL_PROBE_PROB = 0.10  # иногда пропускаем отключённое, чтоб обновить данные

# Минимальный балл сетапа для крипты (максимум 7).
# Жёсткий отбор: сигналов меньше, но качество выше.
MIN_SCORE = 5

# Для валюты максимум 6 (у форекса нет данных по объёму), порог ниже.
MIN_SCORE_FOREX = 4

# Свечи валют старше этого (секунд) = рынок закрыт, пара выбывает
FOREX_MAX_AGE = 180

# Пауза между запросами одного человека (секунд)
USER_COOLDOWN_SECONDS = 5

# Авто-поиск: на какой секунде минуты запускать скан.
# Скан длится 5-9 сек, поэтому стартуем заранее, чтобы сигнал пришёл
# примерно за 20 сек до закрытия минуты (вход — на следующей минуте).
AUTO_SCAN_AT_SECOND = 38

# ---------- Индикаторы (подобраны под 1-минутный скальпинг) ----------

# Быстрый RSI: период 7, границы 25/75 (сильные: 20/80)
RSI_PERIOD = 7
RSI_OVERSOLD = 25
RSI_OVERBOUGHT = 75
RSI_EXTREME_LOW = 20
RSI_EXTREME_HIGH = 80

# Bollinger Bands
BB_PERIOD = 20
BB_STD = 2.0

# Тренд на 5-минутке
EMA_FAST = 9
EMA_SLOW = 21

# Всплеск объёма: текущий объём >= этого множителя от среднего
VOLUME_SPIKE = 1.8

# Фильтр мёртвого рынка: средний размах свечи к цене ниже этого % — пропуск
MIN_ATR_PCT = 0.02         # крипта: 0.02% от цены
MIN_ATR_PCT_FOREX = 0.005  # валюта двигается мельче, порог ниже

# Фильтр новостной свечи: последняя свеча больше ATR в N раз — пропуск
MAX_CANDLE_VS_ATR = 3.5

# Перенос входа на +1 минуту: если живая свеча идёт против сетапа
# телом больше этой доли ATR — разворот не подтверждён, ждём её закрытия.
WAIT_EXTRA_ATR = 0.5

# Быстрый Stochastic: пересечение %K/%D в крайней зоне = разворот начался
STOCH_K = 5
STOCH_SMOOTH = 3
STOCH_D = 3
STOCH_LOW = 30
STOCH_HIGH = 70

# ADX определяет режим рынка:
#   ниже ADX_RANGE        — плоский рынок → тактика ОТКАТА (фейдим края)
#   ADX_RANGE .. ADX_MAX  — тренд → тактика ПРОДОЛЖЕНИЯ (входим по тренду)
#   выше ADX_MAX          — слишком дикий тренд → пропуск
ADX_PERIOD = 14
ADX_RANGE = 22
ADX_MAX = 40
# В тренде: сильный тренд (ADX выше этого) = продолжение надёжнее, +1 балл
ADX_STRONG = 28

# ---------- Котировки Pocket Option (нужен PO_SSID) ----------
# Главный источник: все пары (обычные + OTC) прямо из Покета.
# Если PO_SSID не задан или сессия умерла — бот сам переключается
# на запасные источники (Binance + Deriv).

# Минимальная выплата по паре, % — ниже не берём
MIN_PAYOUT = 85

# Сколько пар максимум анализировать за один скан
# (больше = дольше скан: ~0.7 сек на пару)
MAX_PO_ASSETS = 15

# Свечи старше этого (секунд) = пара не торгуется сейчас, выбывает
PO_MAX_AGE = 180
